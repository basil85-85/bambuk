<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Coming Soon - BAMBUK</title>
   <!-- Standard favicon -->
  <!-- Favicon for all browsers -->
  <link rel="icon" href="/images/logo/favicon-32x32.png" type="image/png">
  <link rel="shortcut icon" href="/images/logo/favicon-32x32.png" type="image/png">
  
  <!-- Apple devices -->
  <link rel="apple-touch-icon" href="/images/logo/favicon-32x32.png">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;700;900&display=swap');
      @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap');
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', sans-serif;
      overflow: hidden;
      background: linear-gradient(135deg, #000000 0%, #000000 50%, #000000 100%);
      position: relative;
    }

    /* Animated background particles */
    .particles {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      z-index: 1;
    }

    .particle {
      position: absolute;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 50%;
      animation: float 6s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% {
        transform: translateY(0px) rotate(0deg);
        opacity: 0.5;
      }
      50% {
        transform: translateY(-20px) rotate(180deg);
        opacity: 1;
      }
    }

    /* Loading screen */
    .loading-screen {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(45deg, #000000, #000000, #000000);
      background-size: 400% 400%;
      z-index: 10000;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      animation: gradientShift 2s ease-in-out infinite, loadingFadeOut 1.5s ease-out 3.5s forwards;
    }

    @keyframes gradientShift {
      0%, 100% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
    }
    
    .loading-logo {
      width: 80px;
      height: 80px;
      border: 4px solid transparent;
      border-top: 4px solid #ffffff;
      border-right: 4px solid rgba(255, 255, 255, 0.3);
      border-radius: 50%;
      animation: loadingSpin 1s linear infinite;
      margin-bottom: 2rem;
      position: relative;
    }

    .loading-logo::before {
      content: '';
      position: absolute;
      top: -8px;
      left: -8px;
      right: -8px;
      bottom: -8px;
      border: 2px solid transparent;
      border-top: 2px solid rgba(255, 255, 255, 0.2);
      border-radius: 50%;
      animation: loadingSpin 2s linear infinite reverse;
    }
    
    .loading-text {
      font-size: 1.5rem;
      font-weight: 300;
      color: #ffffff;
      letter-spacing: 8px;
      opacity: 0.9;
      animation: loadingPulse 2s ease-in-out infinite;
      text-transform: uppercase;
    }
    
    @keyframes loadingSpin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
    
    @keyframes loadingPulse {
      0%, 100% { opacity: 0.5; transform: scale(1); }
      50% { opacity: 1; transform: scale(1.02); }
    }
    
    @keyframes loadingFadeOut {
      0% { opacity: 1; visibility: visible; }
      100% { opacity: 0; visibility: hidden; }
    }

    /* Main content */
    .main-content {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      flex-direction: column;
      z-index: 2;
      position: relative;
    }

    .brand-logo {
      font-size: 1.2rem;
      font-weight: 300;
      color: rgba(255, 255, 255, 0.6);
      letter-spacing: 6px;
      margin-bottom: 4rem;
      opacity: 0;
      transform: translateY(-20px);
      animation: brandFadeIn 1s ease-out 4.5s forwards;
      text-transform: uppercase;
    }

    .coming-soon-title {
      font-size: clamp(2.5rem, 8vw, 6rem);
      font-weight: 900;
      color: #ffffff;
      letter-spacing: clamp(3px, 1vw, 8px);
      display: flex;
      gap: clamp(2px, 0.5vw, 8px);
      margin-bottom: 3rem;
      text-shadow: 0 0 20px rgba(255, 255, 255, 0.3);
    }

    .coming-soon-title span {
      opacity: 0;
      transform: translateY(50px) rotateX(-90deg);
      animation: letterReveal 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
      display: inline-block;
      transition: all 0.3s ease;
    }

    .coming-soon-title span:hover {
      color: #888;
      transform: translateY(-10px) scale(1.1);
      text-shadow: 0 5px 15px rgba(255, 255, 255, 0.4);
    }

    @keyframes letterReveal {
      0% {
        opacity: 0;
        transform: translateY(50px) rotateX(-90deg);
      }
      50% {
        opacity: 0.7;
        transform: translateY(-10px) rotateX(-45deg);
      }
      100% {
        opacity: 1;
        transform: translateY(0) rotateX(0deg);
      }
    }

    @keyframes brandFadeIn {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .subtitle {
      font-size: 1.1rem;
      font-weight: 300;
      color: rgba(255, 255, 255, 0.7);
      letter-spacing: 3px;
      text-align: center;
      opacity: 0;
      transform: translateY(30px);
      animation: subtitleFadeIn 1s ease-out 6s forwards;
      line-height: 1.6;
    }

    @keyframes subtitleFadeIn {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .notification-signup {
      margin-top: 3rem;
      opacity: 0;
      transform: translateY(30px);
      animation: signupFadeIn 1s ease-out 6.5s forwards;
    }

    .signup-form {
      display: flex;
      gap: 1rem;
      align-items: center;
    }

    .email-input {
      background: rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.2);
      border-radius: 50px;
      padding: 12px 24px;
      color: white;
      font-size: 0.95rem;
      width: 280px;
      backdrop-filter: blur(10px);
      transition: all 0.3s ease;
    }

    .email-input:focus {
      outline: none;
      border-color: rgba(255, 255, 255, 0.5);
      background: rgba(255, 255, 255, 0.15);
      box-shadow: 0 0 20px rgba(255, 255, 255, 0.1);
    }

    .email-input::placeholder {
      color: rgba(255, 255, 255, 0.6);
    }

    .notify-btn {
      background: linear-gradient(45deg, #333, #555);
      border: none;
      border-radius: 50px;
      padding: 12px 30px;
      color: white;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-size: 0.9rem;
    }

    .notify-btn:hover {
      background: linear-gradient(45deg, #555, #777);
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(255, 255, 255, 0.2);
    }

    @keyframes signupFadeIn {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Staggered animation delays */
    .coming-soon-title span:nth-child(1) { animation-delay: 4.5s; }
    .coming-soon-title span:nth-child(2) { animation-delay: 4.6s; }
    .coming-soon-title span:nth-child(3) { animation-delay: 4.7s; }
    .coming-soon-title span:nth-child(4) { animation-delay: 4.8s; }
    .coming-soon-title span:nth-child(5) { animation-delay: 4.9s; }
    .coming-soon-title span:nth-child(6) { animation-delay: 5.0s; }
    .coming-soon-title span:nth-child(7) { animation-delay: 5.1s; }
    .coming-soon-title span:nth-child(8) { animation-delay: 5.2s; }
    .coming-soon-title span:nth-child(9) { animation-delay: 5.3s; }
    .coming-soon-title span:nth-child(10) { animation-delay: 5.4s; }
    .coming-soon-title span:nth-child(11) { animation-delay: 5.5s; }

    /* Responsive design */
    @media (max-width: 768px) {
      .brand-logo {
        font-size: 1rem;
        letter-spacing: 4px;
        margin-bottom: 3rem;
      }

      .subtitle {
        font-size: 1rem;
        padding: 0 2rem;
      }

      .signup-form {
        flex-direction: column;
        width: 100%;
        padding: 0 2rem;
      }

      .email-input {
        width: 100%;
        max-width: 300px;
      }
    }

    @media (max-width: 480px) {
      .coming-soon-title {
        letter-spacing: 2px;
        gap: 1px;
      }
      
      .brand-logo {
        letter-spacing: 3px;
      }

      .subtitle {
        font-size: 0.9rem;
      }
    }
    .launch-time {
      font-size: 1rem;
      font-weight: 300;
      color: rgba(255, 255, 255, 0.7);
      margin-top: 1.5rem;
      letter-spacing: 1px;
      text-align: center;
    }

    .countdown-container {
      display: flex;
      gap: 2rem;
      justify-content: center;
      align-items: center;
      margin-top: 2rem;
      opacity: 0;
      transform: translateY(30px);
      animation: countdownFadeIn 1s ease-out 5.8s forwards;
    }

    .countdown-item {
      display: flex;
      flex-direction: column;
      align-items: center;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 15px;
      padding: 1.5rem 1rem;
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.2);
      min-width: 80px;
      transition: all 0.3s ease;
    }

    .countdown-item:hover {
      background: rgba(255, 255, 255, 0.15);
      transform: translateY(-5px);
      box-shadow: 0 10px 30px rgba(255, 255, 255, 0.1);
    }

    .countdown-number {
      font-size: 2rem;
      font-weight: 900;
      color: #ffffff;
      text-shadow: 0 0 10px rgba(255, 255, 255, 0.3);
      margin-bottom: 0.5rem;
      font-family: 'Inter', monospace;
    }

    .countdown-label {
      font-size: 0.8rem;
      font-weight: 300;
      color: rgba(255, 255, 255, 0.7);
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    @keyframes countdownFadeIn {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Responsive countdown */
    @media (max-width: 768px) {
      .countdown-container {
        gap: 1rem;
        flex-wrap: wrap;
        padding: 0 1rem;
      }

      .countdown-item {
        min-width: 70px;
        padding: 1rem 0.8rem;
      }

      .countdown-number {
        font-size: 1.5rem;
      }

      .countdown-label {
        font-size: 0.7rem;
      }
    }
  
.brand-logo-circle {
  width: 120px;
  height: 120px;
  border-radius: 50%; /* makes it circular */
  background: rgba(255, 255, 255, 0.05); /* subtle background */
  display: flex;
  justify-content: center;
  align-items: center;
  margin-bottom: 2rem;
  box-shadow: 0 0 20px rgba(255, 255, 255, 0.2); /* soft glow */
  overflow: hidden; /* ensures logo fits inside circle */
  opacity: 0;
  transform: translateY(-20px);
  animation: logoCircleFadeIn 1s ease-out 4s forwards;
}

.brand-logo-circle img.brand-logo-img {
  width: 70%;
  height: auto;
}

@keyframes logoCircleFadeIn {
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Responsive */
@media (max-width: 768px) {
  .brand-logo-circle {
    width: 100px;
    height: 100px;
    margin-bottom: 1.5rem;
  }
}

@media (max-width: 480px) {
  .brand-logo-circle {
    width: 80px;
    height: 80px;
    margin-bottom: 1rem;
  }
}
  .modal {
      display: none; /* Hidden by default */
      position: fixed;
      z-index: 1000;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      overflow: auto; /* Enable scroll if needed */
      background-color: rgba(0,0,0,0.5); /* Black with opacity */
    }


  @keyframes fadeInBg {
    from {opacity: 0;}
    to {opacity: 1;}
  }

  /* Modal content */
  .modal-content {
    background: #fff;
    max-width: 520px;
    margin: 8% auto;
    padding: 30px 40px;
    border-radius: 16px;
    box-shadow: 0 15px 30px rgba(0,0,0,0.25);
    font-family: 'Poppins', sans-serif;
    color: #333;
    position: relative;
    animation: slideIn 0.4s ease forwards;
  }

  @keyframes slideIn {
    from {
      transform: translateY(-40px);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  /* Close button */
  .close {
    position: absolute;
    top: 18px;
    right: 20px;
    font-size: 28px;
    font-weight: 700;
    color: #999;
    cursor: pointer;
    transition: color 0.3s ease;
  }
  .close:hover {
    color: #e63946;
  }

  /* Heading */
  h2 {
    font-weight: 700;
    font-size: 2rem;
    color: #e63946;
    margin-bottom: 20px;
    letter-spacing: 2px;
    text-transform: uppercase;
  }

  /* Paragraph & features */
  p {
    font-size: 1.05rem;
    line-height: 1.6;
  }

  /* Features list style */
  p strong {
    color: #1d3557;
  }

  /* Emoji icons with text */
  p ul {
    list-style: none;
    padding-left: 0;
    margin-top: 12px;
  }
  p ul li {
    margin-bottom: 12px;
    font-weight: 600;
    font-size: 1rem;
    color: #264653;
    display: flex;
    align-items: center;
  }
  p ul li span {
    font-size: 1.5rem;
    margin-right: 12px;
  }

  /* Button */
  #openModalBtn {
    background-color: #e63946;
    color: white;
    font-family: 'Poppins', sans-serif;
    font-weight: 600;
    border: none;
    padding: 15px 30px;
    border-radius: 50px;
    cursor: pointer;
    font-size: 1.1rem;
    box-shadow: 0 6px 15px rgba(230, 57, 70, 0.5);
    transition: background-color 0.3s ease, box-shadow 0.3s ease;
  }
  #openModalBtn:hover {
    background-color: #d62828;
    box-shadow: 0 8px 20px rgba(214, 40, 40, 0.6);
  }
  </style>
</head>
<body>
  <!-- Animated background particles -->
  <div class="particles" id="particles"></div>

  <!-- Loading screen -->
  <div class="loading-screen">
    <div class="loading-logo"></div>
    <div class="loading-text">BAMBUK</div>
  </div>

  <!-- Main content -->
  <div class="main-content">
    <div class="brand-logo-circle">
  <img src="/images/logo/apple-touch-icon.png" alt="BAMBUK Logo" class="brand-logo-img">

  </div>
    <div class="brand-logo">BAMBUK</div>
    
    <h1 class="coming-soon-title">
      <span>C</span><span>o</span><span>m</span><span>i</span><span>n</span><span>g</span>
      <span>S</span><span>o</span><span>o</span><span>n</span><span>.</span>
    </h1>

   <p class="launch-time" id="launch-time">Launching in:</p>
   
   <div class="countdown-container">
     <div class="countdown-item">
       <div class="countdown-number" id="days">30</div>
       <div class="countdown-label">Days</div>
     </div>
     <div class="countdown-item">
       <div class="countdown-number" id="hours">00</div>
       <div class="countdown-label">Hours</div>
     </div>
     <div class="countdown-item">
       <div class="countdown-number" id="minutes">00</div>
       <div class="countdown-label">Minutes</div>
     </div>
     <div class="countdown-item">
       <div class="countdown-number" id="seconds">00</div>
       <div class="countdown-label">Seconds</div>
     </div>
   </div>
  
  
    <div class="notification-signup">
      <form class="signup-form" onsubmit="handleSignup(event)">
        <input type="email" id="email-value" class="email-input" placeholder="Enter your email" required>
        <button type="submit" class="notify-btn">Notify Me</button>
        
        <button id="openModalBtn" aria-haspopup="dialog" aria-controls="myModal">Learn More About Bambuk</button>
      </form>
    </div>
   
  </div>
   

  //model 

 
<div id="myModal" class="modal" aria-hidden="true" role="dialog" aria-labelledby="modalTitle">
  <div class="modal-content">
    <span class="close" aria-label="Close modal">&times;</span>
    <h2 id="modalTitle">BAMBUK</h2>
    <p>
      Introducing the Bambuk Dress Collection – Arriving <strong>November 21th</strong>!<br><br>
      Step into elegance and style with the brand-new Bambuk dress collection, launching exclusively on our website this November 21th. Designed for the modern trendsetter, each piece combines timeless sophistication with contemporary flair.<br><br>
      <strong>Features of the Bambuk Collection:</strong>
      <ul>
        <li><span>🌿</span>Premium Fabrics: Crafted from luxurious, breathable materials that offer comfort and durability.</li>
        <li><span>👗</span>Versatile Styles: From chic casuals to stunning evening wear, perfect for any occasion.</li>
        <li><span>✨</span>Flattering Fits: Tailored cuts that enhance every silhouette with grace.</li>
        <li><span>🎨</span>Unique Prints & Colors: Stand out with exclusive patterns and a palette inspired by nature and urban vibes.</li>
        <li><span>♻️</span>Eco-Friendly Production: Committed to sustainable fashion with environmentally conscious manufacturing.</li>
      </ul><br>
      Get ready to refresh your wardrobe with Bambuk’s exclusive designs — mark your calendar for November 21th and shop the collection only on our website!
    </p>
  </div>
</div>
  <script>
    const LAUNCH_DATE = new Date('2025-11-22T12:00:00'); 

    function updateLaunchTime() {
      const now = new Date();
      
      const timeRemaining = LAUNCH_DATE.getTime() - now.getTime();
      
      if (timeRemaining > 0) {
        const days = Math.floor(timeRemaining / (1000 * 60 * 60 * 24));
        const hours = Math.floor((timeRemaining % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
        const minutes = Math.floor((timeRemaining % (1000 * 60 * 60)) / (1000 * 60));
        const seconds = Math.floor((timeRemaining % (1000 * 60)) / 1000);
        
        // Update countdown display
        document.getElementById('days').textContent = String(days).padStart(2, '0');
        document.getElementById('hours').textContent = String(hours).padStart(2, '0');
        document.getElementById('minutes').textContent = String(minutes).padStart(2, '0');
        document.getElementById('seconds').textContent = String(seconds).padStart(2, '0');
      } else {
        // Launch time has passed - show "LAUNCHED!" 
        document.getElementById('days').textContent = '00';
        document.getElementById('hours').textContent = '00';
        document.getElementById('minutes').textContent = '00';
        document.getElementById('seconds').textContent = '00';
        document.getElementById('launch-time').textContent = 'LAUNCHED!';
      }
    }

    // Update every second
    setInterval(updateLaunchTime, 1000);
    updateLaunchTime(); // initial call

    // Create floating particles
    function createParticles() {
      const particlesContainer = document.getElementById('particles');
      const particleCount = 50;

      for (let i = 0; i < particleCount; i++) {
        const particle = document.createElement('div');
        particle.className = 'particle';
        
        // Random size and position
        const size = Math.random() * 4 + 1;
        particle.style.width = size + 'px';
        particle.style.height = size + 'px';
        particle.style.left = Math.random() * 100 + '%';
        particle.style.top = Math.random() * 100 + '%';
        
        // Random animation delay
        particle.style.animationDelay = Math.random() * 6 + 's';
        particle.style.animationDuration = (Math.random() * 3 + 3) + 's';
        
        particlesContainer.appendChild(particle);
      }
    }

    // Handle email signup
    async function handleSignup(event) {
    event.preventDefault();

    const email = event.target.querySelector('.email-input').value;

    if (email) {
      const button = event.target.querySelector('.notify-btn');
      const originalText = button.textContent;

      try {
        // send to backend (adjust URL to your server route)
        let response = await fetch("/sending", {
          method: "POST",
          headers: {
            "Content-Type": "application/json"
          },
          body: JSON.stringify({ email })  // send email as JSON
        });

        if (response.ok) {
          button.textContent = 'Thank You!';
          button.style.background = 'linear-gradient(45deg, #4CAF50, #66BB6A)';
        } else {
          button.textContent = 'Try Again!';
          button.style.background = 'linear-gradient(45deg, #e53935, #e35d5b)';
        }
      } catch (err) {
        console.error("Error:", err);
        button.textContent = 'Error!';
        button.style.background = 'linear-gradient(45deg, #e53935, #e35d5b)';
      }

      setTimeout(() => {
        button.textContent = originalText;
        button.style.background = '';
        event.target.querySelector('.email-input').value = '';
      }, 2000);
    }
  }
function validateEmail(email) {
      const emailRegex = /^[a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*$/;
      return emailRegex.test(email);
    }
    // Add hover effects to letters
    document.addEventListener('DOMContentLoaded', () => {
      createParticles();
      
      // Add interactive effects after animations complete
      setTimeout(() => {
        const letters = document.querySelectorAll('.coming-soon-title span');
        letters.forEach(letter => {
          letter.addEventListener('mouseenter', () => {
            letter.style.animation = 'none';
          });
          
          letter.addEventListener('mouseleave', () => {
            setTimeout(() => {
              letter.style.animation = '';
            }, 300);
          });
        });
      }, 6000);
    });


       const modal = document.getElementById("myModal");
        const btn = document.getElementById("openModalBtn");
        const closeBtn = document.querySelector(".close");

        // Open modal
        btn.onclick = function () {
          modal.style.display = "block";
        }

        // Close modal when clicking ×
        closeBtn.onclick = function () {
          modal.style.display = "none";
        }

        // Close modal when clicking outside modal content
        window.onclick = function (event) {
          if (event.target === modal) {
            modal.style.display = "none";
          }
        }
  </script>
</body>
</html>